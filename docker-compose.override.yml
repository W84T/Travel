services:
  app:
    environment:
      - NODE_ENV=development
    volumes:
      - ./apps/web/src:/app/apps/web/src
      - ./apps/api/src:/app/apps/api/src
    ports:
      - "9229:9229"
    command: npm run dev